services:
  # ========== PostgreSQL Database ==========
  postgres:
    image: postgres:16-alpine
    container_name: crypto-payment-gateway-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: gateway
      POSTGRES_PASSWORD: payment_gateway_password
      POSTGRES_DB: payment_gateway
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - payment_gateway_postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gateway -d payment_gateway"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ========== Redis ==========
  redis:
    image: redis:7-alpine
    container_name: crypto-payment-gateway-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - payment_gateway_redis_data:/data
    ports:
      - "127.0.0.1:6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ========== API Server ==========
  api:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: crypto-payment-gateway-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      TZ: UTC
      DATABASE_URL: postgresql+asyncpg://gateway:payment_gateway_password@postgres:5432/payment_gateway
      REDIS_URL: redis://redis:6379/0
    ports:
      - "127.0.0.1:8123:8000"
    volumes:
      - ./data:/data
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ========== EVM Log Poller Worker ==========
  worker-poller:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: crypto-payment-gateway-worker-poller
    restart: unless-stopped
    command: ["python", "-m", "src.workers.evm_log_poller"]
    env_file:
      - .env
    environment:
      TZ: UTC
      DATABASE_URL: postgresql+asyncpg://gateway:payment_gateway_password@postgres:5432/payment_gateway
      REDIS_URL: redis://redis:6379/0
    volumes:
      - ./data:/data
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  # ========== Persistent Deposit Poller (User Wallets) ==========
  worker-persistent:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: crypto-payment-gateway-worker-persistent
    restart: unless-stopped
    command: ["python", "-m", "src.workers.persistent_poller"]
    env_file:
      - .env
    environment:
      TZ: UTC
      DATABASE_URL: postgresql+asyncpg://gateway:payment_gateway_password@postgres:5432/payment_gateway
      REDIS_URL: redis://redis:6379/0
    volumes:
      - ./data:/data
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  # ========== Webhook Dispatcher Worker ==========
  worker-webhook:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: crypto-payment-gateway-worker-webhook
    restart: unless-stopped
    command: ["python", "-m", "src.workers.webhook_dispatcher"]
    env_file:
      - .env
    environment:
      TZ: UTC
      DATABASE_URL: postgresql+asyncpg://gateway:payment_gateway_password@postgres:5432/payment_gateway
      REDIS_URL: redis://redis:6379/0
    volumes:
      - ./data:/data
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  # ========== Unified Sweeper Worker (Single sweep queue for all sources) ==========
  worker-sweeper:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: crypto-payment-gateway-worker-sweeper
    restart: unless-stopped
    command: ["python", "-m", "src.workers.unified_sweeper_runner"]
    env_file:
      - .env
    environment:
      TZ: UTC
      DATABASE_URL: postgresql+asyncpg://gateway:payment_gateway_password@postgres:5432/payment_gateway
      REDIS_URL: redis://redis:6379/0
    volumes:
      - ./data:/data
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  # ========== Invoice Expirer Worker ==========
  worker-expirer:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: crypto-payment-gateway-worker-expirer
    restart: unless-stopped
    command: ["python", "-m", "src.workers.invoice_expirer"]
    env_file:
      - .env
    environment:
      TZ: UTC
      DATABASE_URL: postgresql+asyncpg://gateway:payment_gateway_password@postgres:5432/payment_gateway
      REDIS_URL: redis://redis:6379/0
    volumes:
      - ./data:/data
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

volumes:
  payment_gateway_postgres_data:
  payment_gateway_redis_data:

networks:
  default:
    name: crypto-payment-gateway-network
